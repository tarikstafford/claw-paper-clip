---
phase: 05-telegram-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/validators/chat.ts
  - server/src/services/chat.ts
  - server/src/routes/chat.ts
  - server/src/__tests__/chat-routes.test.ts
autonomous: true
requirements: [TELE-01, TELE-07]

must_haves:
  truths:
    - "sendMessageSchema accepts an optional telegramUpdateId integer field"
    - "chatService.createMessage passes telegramUpdateId through to the DB insert"
    - "Posting a message with a duplicate telegramUpdateId returns HTTP 409 (not 500)"
    - "Posting a message without telegramUpdateId still works (backward compatible)"
  artifacts:
    - path: "packages/shared/src/validators/chat.ts"
      provides: "sendMessageSchema with optional telegramUpdateId"
      contains: "telegramUpdateId"
    - path: "server/src/services/chat.ts"
      provides: "createMessage accepting and inserting telegramUpdateId"
      contains: "telegramUpdateId"
    - path: "server/src/routes/chat.ts"
      provides: "409 response on duplicate telegramUpdateId"
      contains: "409"
    - path: "server/src/__tests__/chat-routes.test.ts"
      provides: "Integration test for TELE-07 dedup"
      contains: "telegramUpdateId"
  key_links:
    - from: "packages/shared/src/validators/chat.ts"
      to: "server/src/routes/chat.ts"
      via: "sendMessageSchema import"
      pattern: "sendMessageSchema"
    - from: "server/src/routes/chat.ts"
      to: "server/src/services/chat.ts"
      via: "svc.createMessage call"
      pattern: "svc\\.createMessage"
---

<objective>
Wire telegramUpdateId through the chat API so the Telegram bot can pass its update_id for dedup, and duplicate IDs return 409 instead of 500.

Purpose: The bot migration (Plan 02) needs the server to accept telegramUpdateId on message creation and handle duplicates gracefully. Without this, TELE-07 dedup cannot work.
Output: Updated shared validator, chat service, chat route with 409 handling, and integration test proving dedup.
</objective>

<execution_context>
@/Users/tarikstafford/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarikstafford/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-telegram-integration/05-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From packages/shared/src/validators/chat.ts:
```typescript
export const sendMessageSchema = z.object({
  body: z.string().min(1).max(50000),
});
export type SendMessage = z.infer<typeof sendMessageSchema>;
```

From server/src/services/chat.ts (createMessage signature):
```typescript
createMessage: async (data: {
  threadId: string;
  senderType: string;
  senderUserId: string | null;
  senderAgentId: string | null;
  body: string;
  processingStatus: string;
}) => { ... }
```

From packages/db/src/schema/chat_messages.ts:
```typescript
telegramUpdateId: bigint("telegram_update_id", { mode: "number" }),
// ...
telegramUpdateIdIdx: uniqueIndex("chat_messages_telegram_update_id_idx").on(table.telegramUpdateId),
```

From server/src/services/documents.ts (existing 23505 pattern):
```typescript
return !!error && typeof error === "object" && "code" in error && (error as { code?: string }).code === "23505";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add telegramUpdateId to sendMessageSchema and wire through chatService</name>
  <files>packages/shared/src/validators/chat.ts, server/src/services/chat.ts</files>
  <action>
1. In `packages/shared/src/validators/chat.ts`, add `telegramUpdateId: z.number().int().optional()` to `sendMessageSchema`. The `SendMessage` type auto-updates via z.infer.

2. In `server/src/services/chat.ts`, update `createMessage`:
   - Add `telegramUpdateId?: number | null` to the `data` parameter type
   - Include `telegramUpdateId: data.telegramUpdateId ?? null` in the `.values()` object passed to `db.insert(chatMessages)`
   - No other changes needed — the column already exists in the schema

This is a backward-compatible change. Existing callers (web UI) do not send telegramUpdateId, so it defaults to undefined/null.
  </action>
  <verify>
    <automated>cd /Users/tarikstafford/Desktop/Projects/claw-paper-clip && npx tsc --noEmit -p packages/shared/tsconfig.json && npx tsc --noEmit -p server/tsconfig.json</automated>
  </verify>
  <done>sendMessageSchema accepts optional telegramUpdateId, chatService passes it to DB insert, TypeScript compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Add 409 dedup handling in chat route and integration test for TELE-07</name>
  <files>server/src/routes/chat.ts, server/src/__tests__/chat-routes.test.ts</files>
  <action>
1. In `server/src/routes/chat.ts`, in the POST `/chat/threads/:threadId/messages` handler:
   - Extract `telegramUpdateId` from `req.body as SendMessage` and pass it to `svc.createMessage({ ..., telegramUpdateId })`
   - Wrap the `svc.createMessage` call in a try/catch. Catch Postgres unique constraint violation (error code `23505`) and return `res.status(409).json({ error: "Duplicate telegram update" })`. Follow the existing pattern from `server/src/services/documents.ts` or `server/src/services/companies.ts` for detecting code 23505.
   - If the error is NOT a 23505, re-throw it so the global error handler picks it up.
   - The rest of the handler (publishLiveEvent, heartbeat wakeup) should only run on successful insert, not on 409.

2. In `server/src/__tests__/chat-routes.test.ts`, add a new describe block "TELE-07: telegram dedup" with two tests:
   - Test A: "POST message with telegramUpdateId succeeds (201)" — send a message with `{ body: "hello", telegramUpdateId: 999001 }`, expect 201.
   - Test B: "POST duplicate telegramUpdateId returns 409" — send the same `telegramUpdateId: 999001` again, expect 409.
   - Follow the existing test patterns in the file (setup, auth mocking, supertest).
  </action>
  <verify>
    <automated>cd /Users/tarikstafford/Desktop/Projects/claw-paper-clip && pnpm test:run -- server/src/__tests__/chat-routes.test.ts</automated>
  </verify>
  <done>Duplicate telegramUpdateId returns 409 (not 500), non-duplicate returns 201, both proven by passing integration tests</done>
</task>

</tasks>

<verification>
- `pnpm test:run -- server/src/__tests__/chat-routes.test.ts` passes with new TELE-07 tests
- TypeScript compiles cleanly for both shared and server packages
</verification>

<success_criteria>
- sendMessageSchema accepts optional telegramUpdateId (number, integer)
- chatService.createMessage inserts telegramUpdateId into DB
- Duplicate telegramUpdateId returns HTTP 409
- All existing chat route tests still pass
- New TELE-07 dedup test passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-telegram-integration/05-01-SUMMARY.md`
</output>
